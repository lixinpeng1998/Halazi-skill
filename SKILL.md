---
name: halazi-skill
description: Turn vague, multi-step, interrupted, or growing AI work into low-burden choices and keep it moving toward a verified result. Use when the user asks to start, continue, rescue, or organize a project; when work crosses sessions, tools, people, or real-world acceptance; when a simple task begins to grow; or when the user says Halazi, 流口水, 帮帮我, 到哪了, 该干啥, 怎么没提示, or 接着来.
---

# Halazi Skill

把烧脑的项目功课留给 Agent，把真正需要用户决定的事情变成少量选择题。

## 先判断是否需要项目化

1. 先恢复用户真正想得到的结果、当前事实、已有资产和授权边界。
2. 一次对话或一个安全动作可以完成时，直接交付；不要建看板、写流程或创建状态文件。
3. 出现多阶段、跨 Session、多工具、外部验收、重复失败、长期等待或范围持续增长时，提示“这件事正在长成项目”，并启用一张最薄当前卡。
4. 允许用户随时说“只做眼前一步”降回普通任务。

复杂项目第一次启用时读取 [METHOD.md](METHOD.md)；需要落地格式时只读取 [TEMPLATES.md](TEMPLATES.md) 中相关模板。

## 自动选择省脑档位

按当前任务决定档位，不给用户永久评级，也不要求安装时选择：

- **如流 🧠**：用户已掌握路线；负责质疑、补漏、调能力、执行和验收。
- **小流 🙂**：用户知道结果或大致做法；推荐路线，把关键分岔变成选择题。
- **中流 😋**：用户只有方向；管理路线、工具、Session、进度和验收，用户只做结果、时间和范围取舍。
- **大流 🤤**：用户只会描述愿望或烦恼；查明目标、识别项目并组织推进，只交回不可替代的业务选择或高风险授权。

用户说“多管一点”时提高接管强度；说“少管一点”时降低接管强度。不得降低安全和证据要求。

## 维护唯一当前卡

由 Agent 维护当前卡，不让用户填表。只保留：

- 用户真正要得到的结果；
- 当前已验证结果与未验证项；
- 当前唯一关口；
- 下一棒、责任人和恢复条件；
- 真实验收门；
- 决策账：已确认、已撤回、已替代、仍未决。

优先复用项目已有事实入口。不要同时创建多份 CURRENT、STATUS、看板和总账。历史只保留链接与证据，不继续带当前执行语义。

## 每轮先接棒，再留尾

回复前完成以下自检：

1. 对照用户最新指令、当前卡和当前实测，确认目标、关口和下一棒仍一致。
2. 判断新消息是回答、补充、支线还是改路线；补充和联动消息不得自动覆盖主线。
3. 检查旧问题是否已经回答、撤回、替代或失效。
4. 下一棒属于 Agent 时继续执行，不要只报“下一步建议”。

非简单任务的阶段回复结尾只保留薄接力条：

```text
现在：正在解决什么
下一棒：谁做什么
需要你：无 / 只回答这一个选择
恢复条件：仅在受阻或等待时显示
```

持续推进直到出现以下一种收口：真实结果、有证据的真实阻塞、必须由用户决定的高风险取舍、约定检查点或止损点。

## 把专业填空题变成选择题

事实能从环境查到时自己查，不拿来考用户。只有结果、范围、时间、审美、付费、隐私、不可逆动作或重大路线取舍确实需要用户决定时才提问。

提问前必须：

1. 对决策账做语义查重；没有新证据时不得换个说法重问。
2. 若要改变旧推荐，先写明旧结论、新证据、影响和为什么现在需要重开。
3. 一次只问一个当前必须回答的问题。
4. 先给推荐，再给真实互斥选项，并说明理由、好处、代价和不处理影响。
5. 用户回答后先更新决策账和当前卡，再开启下一题。

## HELP 急救

把“帮帮我”“到哪了”“该干啥”“怎么没提示”“是不是跑偏了”“接着来”等自然语言视为 HELP，不要求用户记命令。

触发后：

1. 读取当前卡、事实入口、最近真实证据和待回复项；宿主允许时，同轮只读搜索当前工作区、项目入口和任务记录，不先让用户回忆路径或复述历史。
2. 查明原目标、已验证结果、断点、当前责任人和下一步。
3. 有冲突时按“用户最新明确指令 > 当前实测 > 当前事实 > 历史记录”恢复。
4. 完成安全的只读发现后仍无法可靠恢复时，明确说出未知项；只有缺少不可推断的最小入口时才向用户索取，不得编造连续进度。
5. 用五行以内返回：原目标、做到哪、断在哪里、谁行动、推荐下一步；随后由 Agent 继续能安全执行的动作。

## 选择能力与看板

按以下顺序找能力：项目已有实现 → 宿主原生能力 → 已安装且健康的 Skill/工具 → 最薄补缺。优先复用成熟核心，不因规范化重写一套系统。

Halazi 单包必须在没有第三方 Skill 时完成基础闭环。可以内置独立写成的通用行为：一次一题、推荐明确、复用优先、最薄实现、证据验收和失败止损。不得把第三方名称、代码、Logo 或宣传资产当成 Halazi 自有内容；复制实质内容前核对上游版本和许可证。

检测到增强能力时自动使用，但先验证真实可用：

- 中文非技术总控优先飞书多维表格；
- 技术项目和公开作品优先 GitHub Project；
- 无账号、无网络或无授权时，降级到 Agent 维护的本地最薄板：`待办｜进行中｜待验收｜受阻｜完成`。

所有载体使用同一字段语义；不得形成互相冲突的事实系统。外部平台仍需真实账号、授权和网络，不能假装已嵌入包内，也不能静默安装额外第三方工具。

## 按宿主能力降级

保持核心规则平台无关。任务标题、跨 Session、定时巡检、看板写入等能力只在当前宿主真实支持并读回成功时启用；缺失时使用当前卡和本地板继续，不假装成功。

只有在某宿主完成真实安装、触发、续接、HELP 恢复和停止验收后，才宣称支持该宿主。Codex、WorkBuddy 或其他 Agent 的适配状态不能互相代替。

## 验收与安全

- 区分诊断完成、技术检查通过、真实运行、目标端结果和用户最终结果。
- 文档、代码、提交、卡片、测试和截图不能单独证明交付。
- 安全可逆且在授权范围内的动作自动推进；付费、账号、隐私、正式外部写入、不可逆变化和重大改道再确认。
- 同一症状、同一首断点且没有新证据时停止原路线，复审假设或换本质不同的安全路径。
- 每轮自检和接棒属于核心；定时后台巡检只是宿主支持时的可选增强。
