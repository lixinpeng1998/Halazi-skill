# 流口水 Skill 的推进内核与证据边界

流口水式选择题是普通用户看见的交互层；本文件保留从“苦难行军”迁入的完整推进内核。它负责让 Agent 在提问前先恢复事实、选择能力、组织工作包、管理失败并准备真实验收，而不是只把开放题改写成 A / B / C。

## 先看证据标签

`A` 只表示当前项目已经明确确认采用，不表示跨项目普遍有效；`B` 及以下都不能包装成成熟规则。

| 标签 | 含义 | 使用方式 |
| --- | --- | --- |
| `A｜当前明确确认` | 当前项目已明确接受 | 可以在当前项目启用；仍需真实结果验证 |
| `B｜当前实践支持但试运行` | 有当前实践证据，副作用和泛化仍未知 | 作为可选做法，小范围试用并保留未验证项 |
| `C｜仅历史候选不得启用` | 历史上出现过，但没有当前授权或后续曾失效 | 只保留为研究线索，不进入默认流程 |
| `D｜与当前决定冲突并淘汰` | 已与当前边界冲突 | 不得恢复为执行规则 |

## 三层结构

### A. 项目推进内核

- `A｜当前明确确认` 一个项目只维护一个当前事实入口；历史材料只链接，不继续带当前执行语义。
- `A｜当前明确确认` 当前事实必须说清用户最终要得到什么、唯一当前关口、下一步和未验证项。
- `A｜当前明确确认` 只有能独立完成、独立验收且不会争抢同一写入面的任务，才拆成独立工作包。
- `A｜当前明确确认` 每个工作包写清普通用户结果、负责人、完成条件和最低充分证据。
- `A｜当前明确确认` 失败、阻塞、等待对象、恢复触发和责任人必须可见；换一个话题不能让它们消失。
- `A｜当前明确确认` 诊断结论、技术检查、真实运行、目标端结果和用户闭环分层记录；下层通过不能代替上层。
- `A｜当前明确确认` 文档、代码、卡片、提交、测试数量和页面存在都不能单独证明交付。
- `A｜当前明确确认` 最小阶段路线只有一行：恢复目标与事实 → 写出当前关口 → 选择能力路由 → 最薄接入 → 技术检查 → 真实验证 → 结论与写回。
- `B｜当前实践支持但试运行` 当前实测高于当前事实页，当前事实页高于历史记录；发现冲突时先修正当前事实，再继续。

### B. 项目所有者协作约定

- `A｜当前明确确认` 能从环境查到的事实由 Agent 先查；只有真实业务取舍或高风险授权才交给项目所有者。
- `A｜当前明确确认` 每道选择题先给推荐，再给 2—3 个真实可选项，并说明理由、好处、代价和不处理影响；不得用假选项诱导答案。
- `A｜当前明确确认` 项目所有者只需回复选项或纠正目标，不承担需求写作、工具研究、技术路线和验收设计。
- `A｜当前明确确认` 公开版只统一协作字段，实际角色、语言、提问、时间和验收方式以项目双方已填写的协作约定为准；通用示例不能覆盖它。
- `A｜当前明确确认` 不可配置的安全底线只有两类：不得越过授权、隐私或不可逆操作边界；不得用证据不足的结果冒充完成。
- `B｜当前实践支持但试运行` Leo 私人角色示例：项目所有者做业务用途、范围、优先级、审美、付费与高风险授权；Agent 管技术路线、会话、时间、排错、验证与同步。
- `B｜当前实践支持但试运行` Leo 私人提问示例：能查的事实由 Agent 先查；多个有效问题只标一个“现在回答”。公开项目按已填写的提问格式执行。

#### 可填写协作约定

以下六个字段的存在整体属于 `A｜当前明确确认`，具体内容不预设。公开读者填写自己的边界，不复制 Leo 的私人设置；可直接复制的空白版见 `TEMPLATES.md`。

| 字段 | 双方填写什么 | 可填写示例 | 证据档 |
| --- | --- | --- | --- |
| 质疑方式 | 如何提出异议、纠正，以及收到质疑后如何处理 | 直接纠正 / 要求补证 / 暂停路线 | `A｜当前明确确认` |
| 语言 / 汇报粒度 | 使用语言、结果优先程度、技术细节深度 | 只报结果 / 结果加证据 / 完整技术说明 | `A｜当前明确确认` |
| 提问格式 | 一次几题、排序方式、是否附推荐与代价 | 单题 / 少量合并 / 分批处理 | `A｜当前明确确认` |
| 授权边界 | 可自动执行、需确认、禁止执行的动作 | 可逆内部动作 / 外部写入 / 不可逆动作 | `A｜当前明确确认` |
| 时间责任 | 业务截止时间、预算、检查点、等待和止损分别由谁负责 | 固定截止 / 时间盒 / 条件触发 | `A｜当前明确确认` |
| 验收责任 | 技术、真实路径、目标端与业务体验分别由谁判断 | Agent 先验 / 所有者终验 / 联合验收 | `A｜当前明确确认` |

#### 会话六态

以下六态整体属于 `A｜当前明确确认`；这是当前协作约定，不是已经通过自然任务验证的通用结论。

| 状态 | 什么时候用 |
| --- | --- |
| `待回复N` | 仍有 N 个有效问题必须由项目所有者回答；清单放在事实页最底部 |
| `执行中` | 下一动作归 Agent，且正在推进 |
| `待执行` | 下一动作明确，但在等共享资源、账号、设备、写入面或约定窗口 |
| `受阻` | 当前没有安全可行路线，恢复条件尚未出现，也不需要项目所有者决策 |
| `已交付` | 本会话工作包已交付，不代表整个项目完成 |
| `已停止` | 路线已取消、被替代或主动拉停，并保留原因 |

- `A｜当前明确确认` 只要待回复数大于零，标题使用 `待回复N`；Agent 是否仍可继续执行，另写在当前事实中。
- `A｜当前明确确认` 只有问题被明确回答、撤回、替代或事实证明失效时，N 才能减少。

### C. 能力与工具路由

- `A｜当前明确确认` 先查当前项目已有实现和资产，再看平台原生能力、已经可用的工具与依赖，最后才补最薄缺口。
- `A｜当前明确确认` 声称复用时，必须证明真实入口仍经过原成熟核心；读过源码、模仿界面或复制字段不算复用。
- `A｜当前明确确认` 新需求尽量留在成熟核心外围；确需进入核心时，先说明原因、回归面和真实验收门。
- `A｜当前明确确认` 第三方能力只允许提供上游链接、来源、已核验许可证和兼容范围说明；本仓库当前预览不复制第三方 Skill 或代码。
- `B｜当前实践支持但试运行` 一类任务保留一条主路和一个安全兜底；没有新证据时不靠连续换工具制造进展。

> 当前边界：本文件已经提供路由框架，但还没有交付可安装的自动路由实现或逐项验收过的具体 Skill 卡。框架存在不等于能力已经可用。

#### 轻量通用路由卡

匿名类型：`机会`＝个人机会与作品推进，`遗留`＝遗留业务交付，`现场`＝家庭噪声现场实验。路由卡不绑定或复制任何第三方 Skill。

| 阶段 | 触发信号 | 所有者最小输入 | Agent 交付 | 停止 / 切换 | 重叠冲突与顺序 | 证据档 | 三类适用性 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 恢复事实 | 新开、续接或发现状态冲突 | 环境查不到时才补最终结果或最新纠正 | 一个当前事实、一个关口、明确缺口 | 权威来源冲突且无法安全判断时停止 | 当前实测 → 当前事实 → 历史记录 | `A｜当前明确确认` | 机会 / 遗留 / 现场：适用 |
| 选择路由 | 出现新的能力需求或旧路线失效 | 想保留、删除、增加什么及最终怎么用 | `直接复用 / 需要适配 / 不适用 / 最薄接入` 判断与真实基线 | 核心不可用、许可不明或代价超预算时切换 | 项目已有 → 平台原生 → 已可用工具 → 最薄补缺 | `A｜当前明确确认` | 机会 / 遗留 / 现场：适用 |
| 最薄接入 | 路由与边界已经确认 | 仅补业务取舍或高风险授权 | 一个独立工作包、真实入口、完成门和停止点 | 开始重写成熟核心或范围膨胀时停止重评 | 共享账号、设备、写入面保持串行 | `B｜当前实践支持但试运行` | 机会 / 遗留 / 现场：按矩阵适配 |
| 真实验证 | 技术检查通过，准备验证用户结果 | 仅在真人设备、账号或业务判断不可替代时参与 | 真实操作、目标端反查、失败或阻塞证据 | 前置缺失且无安全替代路线时转待执行或受阻 | 诊断 → 技术检查 → 真实路径 → 用户结果 | `B｜当前实践支持但试运行` | 机会 / 遗留 / 现场：证据对象不同 |
| 结论与写回 | 通过、有缺口、失败、阻塞或路线变化 | 只确认业务取舍与最终体验 | 状态收口、剩余未验证、下一责任人 | 四者未一致前不进入下一工作包 | 先收口本项目，再镜像可选总控 | `A｜当前明确确认` | 机会 / 遗留 / 现场：适用 |

## 一次推进循环

1. `A｜当前明确确认` **恢复事实：** 读唯一当前事实入口，并用当前可观察结果核对。
2. `A｜当前明确确认` **写出关口：** 用一句普通用户语言说明这次过关后用户能得到什么。
3. `A｜当前明确确认` **选工作包：** 只保留眼下能独立完成和验收的工作；未来范围留在关口之后。
4. `A｜当前明确确认` **执行与止损：** Agent 管技术路线、时间和失败处理；重复动作没有新证据时换本质不同的安全路径或停止。
5. `A｜当前明确确认` **分层验收：** 分别记录诊断、技术检查、真实运行、目标端结果和用户结果。
6. `A｜当前明确确认` **写回：** 只在状态、关口、决定、阻塞或真实结果变化时覆盖更新当前事实。
7. `A｜当前明确确认` **交给所有者：** 只有业务选择或高风险授权确实成为下一关时才提问，并附推荐、理由、代价和不处理影响。

## 可选增强

`A｜当前明确确认` 这些不是最小核心，缺少它们不影响使用本方法。

| 增强 | 证据档 | 边界 |
| --- | --- | --- |
| 私人日常总控 | `B｜当前实践支持但试运行` | 只聚合各项目最小状态，不接管业务事实 |
| GitHub Project | `B｜当前实践支持但试运行` | 作为公开路线和工作包视图，不替代项目当前事实 |
| 风险预警、进入验收、最终结论三个时间点 | `B｜当前实践支持但试运行` | 没有可靠时间就留空，不能猜填 |
| 高风险独立复核 | `B｜当前实践支持但试运行` | 只在不可逆、隐私、外部写入或重要完成结论时使用 |
| 每日静默巡检 | `B｜当前实践支持但试运行` | 无变化不改、不通知；只核对新鲜度和冲突 |

`A｜当前明确确认` 公开指南与私人总控只互相说明职责和入口，不做双向同步。

## 不启用与已淘汰

- `C｜仅历史候选不得启用` 固定限制所有项目只能有某个工作包数、角色数或统一时间盒。
- `C｜仅历史候选不得启用` 默认安装 Skill、建立事故库、测试题库或长期后台来保证执行。
- `C｜仅历史候选不得启用` 把一次受控复核通过扩大成跨项目长期有效。
- `D｜与当前决定冲突并淘汰` 用主观百分比表示项目完成度。
- `D｜与当前决定冲突并淘汰` 把目录存在、Session 数、规则数量或看板字段数当成项目进度。
- `D｜与当前决定冲突并淘汰` 给全部项目设置固定的全局并行上限。
- `D｜与当前决定冲突并淘汰` 让项目所有者手工同步状态、协调 Agent 或判断技术根因。
- `D｜与当前决定冲突并淘汰` 把诊断、静态测试、截图、模拟数据或候选文件写成真实交付。

## 当前验证边界

- `B｜当前实践支持但试运行` 上游“苦难行军”Part 1 正在个人机会与作品推进、遗留业务交付、家庭噪声现场实验三类匿名目标项目中做最薄接入与真实验证，目前均未完成收口。
- `B｜当前实践支持但试运行` Halazi Skill 迁入这些方法，只证明文档和交互设计存在；不能把上游试运行写成 Halazi 已经真实通过。
- `B｜当前实践支持但试运行` 私人总控、公开项目视图、时间三节点、高风险复核和静默巡检都是可选增强，尚未证明长期收益大于维护成本。
- `C｜仅历史候选不得启用` 其他匿名历史案例只能帮助适配，不能替代当前三类项目的真实验证。

`B｜当前实践支持但试运行` 在上游三类目标项目的自然变化能够无需所有者提醒地保持事实、关口、阻塞和待回复清单一致，且 Halazi 自己完成真实使用验证之前，只能称“试运行”。
